# ELİŞA - Core dependencies (all free, pip install)
# Python 3.9+

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
webrtcvad>=2.0.10
numpy>=1.24.0
scipy>=1.10.0

# STT - faster-whisper (offline)
faster-whisper>=1.0.0

# TTS - Piper (offline) + fallback
piper-tts>=1.2.0
pyttsx3>=2.90

# LLM - Ollama client (HTTP, no API key)
requests>=2.31.0
pyyaml>=6.0

# Utils
psutil>=5.9.0
ddgs>=9.0.0

# macOS masaüstü uygulaması (Siri tarzı gizli panel + menü çubuğu)
pywebview>=4.0; sys_platform == "darwin" or sys_platform == "win32"
rumps>=0.4.0; sys_platform == "darwin"

# Wake word - openwakeword OPSİYONEL: tflite-runtime gerektirir,
# Python 3.9 + macOS'ta kurulamadığı için STT tabanlı wake kullanılıyor.
# Kurmak istersen: pip install openwakeword tflite-runtime
# openwakeword>=0.6.0

# Android / Kivy (only for APK build, not needed for desktop)
# kivy>=2.3.0
# kivymd>=1.1.1
# buildozer>=1.5.0
